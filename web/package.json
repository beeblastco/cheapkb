{
  "name": "cheapkb-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "BROWSERSLIST_IGNORE_OLD_DATA=true tailwindcss -i ./input.css -o ./.styles.css --content ./index.html ./main.js --minify && node build.js",
    "dev": "npm run build && cd dist && python3 -m http.server 5173"
  },
  "devDependencies": {
    "pdfjs-dist": "^6.1.200",
    "tailwindcss": "^3.4.19",
    "terser": "^5"
  }
}
