{
  "name": "cheapkb-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js && cd dist && python3 -m http.server 5173"
  },
  "devDependencies": {
    "terser": "^5"
  }
}
